name: fruits
on:
  workflow_dispatch:

env:
  FRUITS: [pomme, bannane, pêche, poire]
  NOMS: [Theo, Valentin, Adrien]

jobs:
  fruits_matrix:
    strategy:
      fail-fast: true
      matrix:
        fruits: $FRUITS
        noms: $NOMS
    runs-on: ubuntu-latest
    steps:
      - name: Affiche la combinaison
        run: echo "${{ matrix.noms }} mange une ${{ matrix.fruits }}"